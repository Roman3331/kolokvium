Рядок № 13 та 53.

<!DOCTYPE html>
<html>
  <head>
    <title>Система продажу нерухомості (Варіант 11)</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body class="bg-light">

    <!-- Навігація (як на рис. 6.2–6.3) -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
      <div class="container">
        <a class="navbar-brand fw-bold" href="/">Нерухомість</a>
        <div class="navbar-nav ms-auto d-flex align-items-center">
          <%= link_to t('all_properties'), properties_path, class: "nav-link text-white" %>
          <%= link_to t('add_property'), new_property_path, class: "nav-link text-white" %>
          <%= link_to "EN", url_for(locale: :en), class: "btn btn-sm #{I18n.locale == :en ? 'btn-primary' : 'btn-outline-light'} mx-1" %>
          <%= link_to "UK", url_for(locale: :uk), class: "btn btn-sm #{I18n.locale == :uk ? 'btn-primary' : 'btn-outline-light'} mx-1" %>

          <% if user_signed_in? %>
            <span class="text-white mx-3"><%= t('welcome', email: current_user.email) %></span>
            <%= button_to t('log_out'), destroy_user_session_path, method: :delete, class: "btn btn-outline-light btn-sm" %>
          <% else %>
            <%= link_to t('log_in'), new_user_session_path, class: "btn btn-outline-light btn-sm me-2" %>
            <%= link_to t('sign_up'), new_user_registration_path, class: "btn btn-light btn-sm" %>
          <% end %>
        </div>
      </div>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div class="container">
      <!-- Повідомлення про помилки/успіх -->
      <% flash.each do |type, msg| %>
        <div class="alert alert-<%= type == 'notice' ? 'success' : 'danger' %> alert-dismissible fade show">
          <%= msg %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <%= yield %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
